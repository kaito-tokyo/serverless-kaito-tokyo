name: "On push"

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

permissions:
  contents: "read"

jobs:
  PushPrettier:
    uses: "./.github/workflows/push-prettier.yaml"

  PushStrapiWrangler:
    uses: "./.github/workflows/push-strapi-wrangler-build.yaml"
